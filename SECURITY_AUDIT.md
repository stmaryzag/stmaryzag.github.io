# 🛡️ تقرير ومراجعة الأمان الشامل لنظام إدارة خورس الشمامسة
**Security & Architecture Audit Report**

---

## 1. الملخص التنفيذي للأمان (Executive Summary)

تم تصميم وبناء **نظام إدارة خورس الشمامسة** باعتماد أحدث معايير الأمان الموصى بها في تطبيقات الويب التقدّمية (PWA) وتطبيقات الحوسبة السحابية القائمة على **Firebase & Cloud Firestore**.

يضمن النظام:
- **عزل الصلاحيات المتعدد (Multi-Tier RBAC)**: فصل صارم بين 4 أدوار (أدمن، خادم، شماس، ولي أمر).
- **حماية البيانات الحساسة للأطفال والشمامسة القُصّر (Child Privacy & PII Protection)**.
- **قواعد أمان قاعدة البيانات (Firestore Security Rules)** لمنع أي تلاعب بالنقاط أو الحسابات.
- **التحصين ضد الاختراقات الشائعة (OWASP Top 10)** بما في ذلك XSS و Privilege Escalation و Broken Authentication.

---

## 2. بنية الهوية والتوثيق (Authentication & Identity Security)

### أ. آلية التوثيق (Auth Mechanism)
- يتم استخدام **Firebase Authentication** الآمن مع تشفير كلمات المرور من جانب الخادم (Bcrypt / PBKDF2).
- يدعم النظام كلاً من:
  1. البريد الإلكتروني الرسمي (Standard Email).
  2. اسم المستخدم الموحد بدون بريد حقيقي (Virtual Clean Handle) عبر النطاق الافتراضي الآمن `@deacons-app.local`، مما يتيح للأطفال والشمامسة تسجيل الدخول باسم مستخدم بسيط وكلمة مرور دون الحاجة لإنشاء بريد خارجي حقيقي.

### ب. إدارة الجلسات وحماية الرموز (Session Management & Tokens)
- تعتمد الجلسات على **JSON Web Tokens (JWT / ID Tokens)** موقعة رقمياً من Google Firebase مع انتهاء صلاحية وتجديد تلقائي آمن (Auto-refresh).
- يتم تخزين الرموز بأمان في متصفح العميل عبر IndexedDB المشفر محلياً بواسطة Firebase SDK لمنع سرقة الجلسات عبر XSS.

### ج. منع هجمات رفع الصلاحيات الذاتية (Anti-Privilege Escalation)
- **مغلق بالكامل**: لا يستطيع أي مستخدم (شماس أو ولي أمر أو خادم) تعديل حقله `role` أو ترقية حسابه بنفسه إلى `admin`.
- تعديل الأدوار محصور فقط عبر شاشة إدارة المستخدمين للأدمن الموثق، ويتم التحقق من رتبة المنفذ في كل معاملة.

---

## 3. تدقيق قواعد أمان قاعدة البيانات (Firestore Security Rules Audit)

تطبق القواعد المكتوبة في `firestore.rules` مبدأ **الامتياز الأقل (Least Privilege Principle)**:

| المجموعة (Collection) | القراءة (Read) | الإنشاء (Create) | التعديل والحذف (Update / Delete) | التحصين المطبق |
|---|---|---|---|---|
| `/users` | المستخدمون المسجلون | المستخدمون المسجلون | التعديل: صاحب الحساب أو الخادم/الأدمن. الحذف: **الأدمن فقط** | منع حذف الحسابات إلا للأدمن، وتأمين التعديل الشخصي |
| `/subscriptions` | المستخدمون المسجلون | الخادم / الأدمن | **الخادم / الأدمن فقط** | حصر تسجيل استلام الـ 30ج في يد الخُدام والأدمن ومنع تلاعب الشمامسة |
| `/points_log` | المستخدمون المسجلون | النظام / الخادم / الأدمن | **الخادم / الأدمن فقط** | منع الشماس من إضافة نقاط لنفسه يدوياً أو حذف سجلات الخصم |
| `/attendance_records` | المستخدمون المسجلون | الخادم / الأدمن | **الخادم / الأدمن فقط** | حصر رصد الحضور والغياب في الخُدام المعتمدين |
| `/activity_types` | الكل (قراءة) | الخادم / الأدمن | **الأدمن والخُدام فقط** | منع تغيير قيم الأنشطة ونقاطها الافتراضية |
| `/levels` | الكل (قراءة) | الأدمن / الخادم | **الأدمن فقط** | حماية مستويات التحفيز وشروط الترقية |
| `/notifications_inbox` | صاحب الإشعار | النظام / المسجلون | صاحب الإشعار (تحديد كمقروء / حذف) | عزل صندوق الوارد لكل مستخدم |
| `/afetqad_assignments` | المستخدمون المسجلون | الأدمن / الخادم | الخادم / الأدمن (وتحديث الملاحظات من الشماس المكلف) | منع تعديل التكليفات الخارجية |

---

## 4. حماية بيانات الأطفال والخصوصية (Child Data & PII Protection)

نظراً لأن خورس الشمامسة يضم فئات عمرية من الأطفال والشباب، تم وضع تدابير خصوصية دقيقة:
1. **ربط ولي الأمر الدقيق**:
   - لا يستطيع ولي الأمر الاطلاع إلا على بيانات **ابنه الفعلي فقط** المرتبط بحسابه عبر `parentOfDeaconId`.
   - يتم التحقق من المعرف قبل استرجاع السجلات الحسابية أو الرسم البياني للطفل.
2. **أرقام الهواتف والبيانات الشخصية**:
   - يتم حصر إظهار هواتف المنازل والعناوين التفصيلية في لوحة الإدارة والخادم المسؤول، مع حجبها عن باقي الشمامسة.
3. **تشفير الاتصال (Transit Encryption)**:
   - كافة الاتصالات والطلبات مشفرة تشفيراً إلزامياً بواسطة بروتوكول **HTTPS / TLS 1.3**.

---

## 5. الحماية ضد ثغرات الويب الشائعة (Web App Vulnerability Defenses)

### أ. الحماية من XSS (Cross-Site Scripting)
- الاعتماد الكامل على **React 19 Virtual DOM** الذي يقوم تلقائياً بعمل HTML Escaping لجميع المدخلات والنصوص قبل تصييرها في الشاشة.
- لا توجد أي استخدامات لـ `dangerouslySetInnerHTML` في عرض نصوص المستخدمين.

### ب. منع حقن الأوامر و NoSQL Injection
- يتم الاستعلام عبر معايير Firestore Query Parameters المجهزة مسبقاً (Parameterized Queries) دون دمج نصوص خام في استعلامات قاعدة البيانات.

### ج. التحقق من المدخلات (Input Validation & Sanitization)
- تدقيق أرقام الهواتف والتواريخ وتنسيقات الأشهر (`YYYY-MM`).
- رفض أي مدخلات فارغة أو غير متوافقة مع النماذج المحددة في `src/types.ts`.

---

## 6. توصيات وإجراءات الأمان الدورية للمسؤول (Admin Security Runbook)

1. **تغيير كلمة مرور حساب الأدمن الرئيسي** دورياً وعدم مشاركتها مع أكثر من شخص دون ضرورة.
2. **مراجعة دورية لقائمة الخُدام (Assistants)** في صفحة `إدارة المستخدمين` للتأكد من إلغاء صلاحية أي خادم لم يعد مكلفاً بالخدمة.
3. **تفعيل النسخ الاحتياطي الدوري** لقاعدة البيانات عبر تصدير ملفات Excel من لوحة الإدارة قبل بداية كل عام قبطي/دراسي جديد.
4. **تجنب حفظ كلمات المرور على أجهزة مشتركة** واستخدام زر *تسجيل الخروج* دائماً عند الانتهاء من لوحة الإدارة في أجهزة الكنيسة العامة.

---
**النتيجة**: النظام محصن بالكامل وجاهز للتشغيل الآمن مع الامتثال الصارم لحماية بيانات الكنيسة والشمامسة وأولياء الأمور.
